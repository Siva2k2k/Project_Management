{
  "name": "pm-dashboard",
  "version": "1.0.0",
  "description": "Project Management Dashboard - Full Stack Application",
  "scripts": {
    "install": "cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build && cd ../server && npm run build",
    "start": "cd server && npm start",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
	"heroku-postbuild": "cd client && npm install --include=dev && npm run build && cd ../server && npm install --include=dev && npm run build"

  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "keywords": ["project-management", "dashboard", "react", "express"],
  "author": "",
  "license": "ISC"
}
